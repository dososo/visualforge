# 变更记录

## 0.5.8 一次生成、即时宫格与开源首发（2026-08-06）

- 每个套图／宫格画面成功生成一次即完成；质量检查只保留建议，不再暗中创建重试任务或把有效图片改成失败。
- 选定最终作品时原子更新候选、当前输出、单格状态、整组计数和 Final Selection；最终宫格按真实可用图片即时合成，不再依赖关闭侧边栏后的恢复刷新。
- 本地宫格合成失败会停止等待并提供“重新合成宫格”，该动作只重跑本地合成，不重新生成图片。
- 建立不含内部历史、真实网站证据、人物／商品原图、本机路径和过程文档的干净公开源码快照。
- 新增中英文 README、最小安装指南、真实插件运行截图、GitHub Pages 首页与 macOS／Windows／Linux 统一下载页。

## 0.5.7 参考画面锚定与最小正式包（2026-08-06）

- 待复刻画面升级为强约束锚点：首张严格复刻，后续套图只允许最多两个相邻变化，并锁定服装、妆发、场景、光影、色彩与后期。
- 人物／商品素材只负责主体身份；Critic 增加动作、表情、服装、构图、光影与整组连续性，Retry 以失败候选为底图定向修复。
- 输入宫格裁切与最终组合布局分离；结果页先单张后宫格，横／竖切换只做本地重排。
- 普通用户交付收敛为一个签名、公证并 Staple 的 Universal DMG 与一份 SHA-256，不再外发重复 Host ZIP、Chrome ZIP、公证 JSON或中间产物。

## 0.5.6 参考保真与宫格结果闭环（2026-08-05）

- 将待复刻参考图的光向、曝光、高光、色彩、反差、景深、镜头感、材质与后期观感升级为整组生成硬约束，单格镜头计划只覆盖明确变化维度，降低套图对原图氛围、质感和画面元素的漂移。
- 人物生成新增头身比、躯干长度、腰线、膝位、腿长和自然透视约束；质量检查可识别大头小身、短躯干、短腿及透视压缩造成的比例失真。
- 宫格生成前明确选择保持原布局、横向或纵向排版；完成后主结果直接展示合成宫格，单格图保留为查看、选择和定向重试入口。
- 套图规划与完成态持续展示待复刻画面及全部人物／商品身份素材；图片查看器让完整图像避开顶部、底部工具栏，并恢复高对比操作文字。
- 扩展、Native Host、兼容下限和发行文件统一升级为 0.5.6；正式 DMG 必须绑定本次源码提交并重新完成 Developer ID 签名、Apple 公证与 Staple。

## 0.5.5 DMG Finder 可见性修复（2026-08-05）

- 修复从隐藏 Git worktree 产包时，`hdiutil` 因源路径包含隐藏祖先目录而把 DMG 全部内容标成 `hidden`，导致文件存在但 Finder 显示空白的问题。
- macOS staging 现在固定创建在系统临时目录，脱离隐藏 worktree；同时显式清除发行文件隐藏标志，并在发布前真实挂载 DMG、逐项反查 Finder 可见性。
- 0.5.4 公证包因普通用户无法从 Finder 找到安装入口而作废；0.5.5 必须重新构建、签名、公证、Staple 并生成全新 SHA-256。

## 0.5.4 普通用户验收候选（2026-08-05）

- 单张创作将捕获或上传的原图作为“待复刻画面”，锁定具体构图、景别、气质、背景、道具、动作、光影、色彩和材质，只替换用户明确选择的人物或商品。
- 人物卡的 1～5 张原始身份照片全部进入生成联合证据；“身份主照片”仅作为首要脸部锚点，不再暗中丢弃第 4／5 张照片。
- 人物素材文案统一为“身份主照片／设为身份主照片”，与待复刻作品彻底区分；照片质量和多图一致性只作建议，不阻止用户保存。
- 新增 `generation-style-layout-v1` Host 能力门；0.5.4 扩展不会继续使用缺少待复刻画面职责的旧 Host。
- 保留 0.5.3 的任务完成、单张数量、结果主操作和全插件交互收口，并重新绑定当前源码的签名、公证和分发字节。

## 0.5.3 本地验收候选（2026-08-04）

- 已完成作品不再出现在“继续上次创作”；创作首页只恢复真正未完成的任务，后台完成后返回创作页不会再进入空白或伪“生成中”状态。
- 单张作品默认只生成 1 张，用户可在主流程直接选择 1／2／4 张；按钮、进度、返回结果和任务终态统一使用用户明确选择的真实数量。
- 质量检查不再暗中追加修复图并延长“生成中”；图片保存后任务即结束，发现问题时由用户明确发起定向修复。
- 结果页按状态只保留一个主操作：未选定时优先 Final Selection，选定后优先导出；主体缺失提醒打开时隐藏底层生成操作。
- 参考图页将低频的保留／替换细节与风格方向收进统一“更多调整”，并完成 360／420／560px 文案、焦点、触达区与无横向溢出复核。
- 扩展、Native Host、兼容下限和跨平台发行包统一升级为 0.5.3；正式签名、公证与目标系统验收继续以对应发行字节为准。

## 0.5.1 本地验收候选（2026-08-03）

- 统一单图、普通套图与全部宫格的数量、主体替换、计划、进度、取消、失败、恢复、查看、Final Selection 和导出语义；二宫格不再被强制扩成三张，选定数量不会在下一页漂移。
- 宫格逐格裁切后立即生成可编辑基础计划，每格持久化构图、景别、动作和情绪；支持 2／3／4／6／9／12 格、横向／纵向／非等分布局、低置信度确认和非宫格恢复。
- 人物引用改为脸部与全身体型优先，明确排除风格参考人物身份；动作降级、负向控制、分项质量门和最多一次定向修复共同降低串脸、体型漂移与诡异肢体。
- 质量状态不再伪通过：Critic 缺失／失败保持“尚未检查”，只有真实报告可标记已检查；每张最终版本必须由用户明确选择，部分完成也可保留、选择并导出。
- 修复套图并发写入覆盖、恢复／重试快照失效、部分 Manifest 数量失真和陈旧 Project 覆盖；生成、事件、Manifest、任务与 Project 采用原子提交或事务内合并。
- Hover／框选／粘贴／设置／通知／长任务反馈、查看器焦点和 360～560px 栅格完成全插件收口；安全可理解的失败原因保留，技术栈消息继续隐藏。
- Native Host 增加 0.5.1 握手能力、运行心跳、孤儿恢复、存储预算与完整性修复；发行包绑定源码提交、扩展 ZIP 哈希、渠道、扩展 ID、架构和安装结果。
- macOS arm64／Intel x64、Windows x64、Linux x64 的候选打包与下载页源码已建立；Universal 构建继续由 Developer ID 双切片门禁保护。正式商店 ID、签名／公证、公开下载页部署、目标系统真机、真实 Chrome 与 Product Reality Validation v2 仍是独立发布门禁。

## 0.5.0 本地验收候选（2026-08-03）

- 宫格主流程不再等待远程逐格规划：本机裁切、保存后立即用基础构图／景别／动作／情绪进入可编辑规划；保留 Host 75 秒和客户端 90 秒作为非主路径防御上限。
- 正式支持上下／左右二宫格识别、逐格生成与合成；识别后直接提示“复刻这张二宫格”，默认画面数与参考布局一致。
- 生成规格改为“生成画面数／最终保存什么”，明确区分独立图、宫格成图和两种都保存；规划页优先确认已选数量，修改入口默认收起。
- 网页图片按钮先确认“已接收”再后台捕获；失败时重试原操作，不再无提示等待或偷偷切换为框选模式。
- 全插件主次动作与结果文案统一：宫格使用“直接复刻／换成我的再复刻／作为单张创作”，结果使用“调整这张作品／再生成一个版本”，导出名称与实际文件一致。
- 修复普通商品边缘误判宫格；分隔线必须具备跨轴连续覆盖。辅助文字达到正常文本 AA 对比度，紧凑删除按钮扩大到 28×28px。
- 移除遮挡主体提醒的重复 Toast；单图、套图、等待、失败、结果、作品和设置在 360／420／560px 完成当前截图复核。
- 当前安装包仍属于本地验收候选；真实 Chrome、真实 Codex 逐格质量、正式签名／公证和 Product Reality Validation v2 继续单独验收。

## 0.4.0 本地验收候选（2026-08-03）

- 人物身份参考改为脸部、全身优先，风格／构图参考中的人物明确排除为身份来源，降低参考人物串脸和体型漂移。
- 高风险交叉肢体动作自动降级为低遮挡安全姿态；加入肢体、关节、持物、身份与表情负向控制，并由 Critic 复核。
- 已生成图片不再因质量检查服务异常被误判失败，自动定向重试由两次收敛为一次，减少无效等待。
- 套图结果页优先展示当前单张大图，主要操作集中，创作依据、调整、导出、质检、克隆和删除按层级收口。
- 当前三平台包仍属于本地验收候选；真实人物相似度、目标系统运行、正式签名／公证与 Product Reality Validation v2 继续单独验收。

## 0.3.0 本地验收候选（2026-08-03）

- 完成三宫格逐格语义、主体替换入口、当前页规划、持续进度、合成导出与恢复路径。
- 人物面部／全身参考职责分离，增加人脸、体型、动作、表情质量门与有界定向修复。
- 补齐套图命名、Final Selection、后台通知、Hover 状态、Windows／Linux Native Host 候选包。
- 当前三平台包仍属于本地验收候选；正式签名、公证、目标系统真机与 Product Reality Validation v2 继续单独验收。

## 0.1.0 源码开发者预览（2026-08-02）

- 准备 Apache-2.0 GitHub 源码开源边界；排除签名二进制、真实网站视频、真人／商品验收图片和历史交付包。
- 浏览器 E2E 的公开主路径改用虚构测试夹具，不依赖私有验收素材。
- Chrome Web Store 尚未提交审核；当前不是普通用户一键安装正式版。
- 历史 DMG 公证只证明对应历史字节，不作为当前源码构建的发布证明。

## 0.1.0 Distribution Readiness v1

- 增加 macOS arm64 自包含 Native Host 与固定安装入口，不再依赖用户 Node 或源码目录。
- 增加确定性 Codex Discovery、共享 Native Messaging Zod 契约和过期 Host 缓存安全清理。
- 独立分发候选包仍待 Developer ID 签名、Apple 公证与 x64 真机验收。

## 0.1.0-rc.1

- 完成 Chrome MV3 Side Panel、Native Host 与真实 Codex imagegen 闭环。
- 建立 Visual DNA、Revision、GenerationEvent、Provenance 和 Task Lifecycle。
- 完成网页捕获、框选截图、Visual DNA Editor、Preference Center。
- 加入首次使用、真实状态、错误恢复、结果层级和键盘可访问性加固。
- 发布前收窄扩展权限，改为用户触发时动态注入框选脚本。
- 增加 Native 输出路径、符号链接、图片类型、大小与成功任务文件清理保护。

当前发布范围为 macOS 源码安装版；Chrome 原生右键菜单仍需人工签收。
